import { ReactNode } from "react"

export interface IWrapperDefaultProps {
    children?: ReactNode
    showDefault?: boolean
    classNameWrapper?: string,
    classNameDefault?: string,
    className?: string,
}